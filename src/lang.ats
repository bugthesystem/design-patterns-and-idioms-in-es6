export function int() {
};
int.assert = function (value) {
    return value == null || typeof value == 'number' && value === Math.floor(value);
};

export function float() {
};
float.assert = function (value) {
    return value == null || typeof value == 'number';
};